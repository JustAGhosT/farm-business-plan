'use client'

// Re-export from split modules for backward compatibility
export { useFormValidation } from '@/lib/hooks/useFormValidation'
export { FormInput, FormTextarea } from '@/components/ui/FormFields'

